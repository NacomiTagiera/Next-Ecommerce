import { faqs } from "./faqs";

# Frequently Asked Questions

Have a different question and can’t find the answer you’re looking for? Reach out to our support team by <a href="mailto:support@next-sports.com" className="font-semibold text-skyfall-600 hover:text-skyfall-500">sending us an email</a> and we’ll get back to you as soon as we can.

<dl className="mt-10 space-y-8 divide-y divide-zinc-400">
	{faqs.map((faq) => (
		<div key={faq.id} className="pt-8 lg:grid lg:grid-cols-12 lg:gap-8">
			<dt className="text-base font-semibold leading-7 text-zinc-900 lg:col-span-5">
				{faq.question}
			</dt>
			<dd className="mt-4 lg:col-span-7 lg:mt-0">
				<p className="text-base leading-7 text-zinc-600">{faq.answer}</p>
			</dd>
		</div>
	))}
</dl>
