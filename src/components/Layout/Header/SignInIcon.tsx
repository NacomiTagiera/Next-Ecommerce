import { FiUser } from "react-icons/fi";

import { IconButton } from "@/components/UI/IconButton";

export const SignInIcon = () => <IconButton href="/sign-in" icon={FiUser} hiddenLabel="Sign in" />;
